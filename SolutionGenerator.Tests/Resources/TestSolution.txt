solution MySolution
{
    configurations =
    {
        everything =
        {
            Debug = [debug,test]
            Release = [release]
        }

        no-tests =
        {
            Debug = [debug]
            Release = [release]
        }
    }

    target platforms =
    [
        Any CPU,
    ]

    settings template.defaults
    {
        root namespace = $(SOLUTION_NAME).$(PROJECT_NAME)
        module source path = Resources/$(MODULE_NAME)
        include files = glob ".{cs,txt,json,xml,md}"
        exclude files = none
        lib refs = none
        project refs = none
        define constants = none
        target framework = v4.6
        language version = 6
        debug symbols = true
        debug type = full
        optimize = false
        error report = prompt
        warning level = 4
        platform target = AnyCPU
    }

    include templates =
    [
        glob "Resources/Templates/**/*.template"
    ]

    include modules =
    [
        glob "Resources/**/*.module"
    ]
}
